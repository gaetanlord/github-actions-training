name: 03 - Workflow Runners

on: 
  push:
  workflow_dispatch:


jobs:
  ubuntu-echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show OS
        run: |
          echo "I've been running on $RUNNER_OS OS."